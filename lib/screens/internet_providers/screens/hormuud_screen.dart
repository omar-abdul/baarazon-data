import 'package:flutter/material.dart';

class HormuudScreen extends StatelessWidget {
  const HormuudScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
